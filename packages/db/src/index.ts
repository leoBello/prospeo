// Types générés depuis la base réelle par `pnpm db:types`.
// Ne pas éditer database.types.ts à la main : il est régénéré à chaque
// migration, et toute retouche serait écrasée sans avertissement.
export type { Database, Json, Tables, TablesInsert, TablesUpdate, Enums } from './database.types.js';
